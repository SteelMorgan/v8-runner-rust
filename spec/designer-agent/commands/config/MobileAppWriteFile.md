# MobileAppWriteFile

## Назначение

Сохраняет мобильное приложение в `.xml`-файл.

## Синтаксис

```text
config mobile-app-write-file --file=<file>
```

## Параметры

- `--file=<file>` — путь к результирующему файлу.

## Связи

- Работает в группе `config`.

## Примечания

- Подтверждено встроенным `help config` в SSH shell `8.5.1.1150`.
